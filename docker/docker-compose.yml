
services:
  api:
    build: ..
    ports: ["8000:8000"]
    env_file: ../.env
    command: uvicorn rl_pdm.app.api_server:app --host 0.0.0.0 --port 8000
  dashboard:
    build: ..
    ports: ["8501:8501"]
    env_file: ../.env
    command: python -m rl_pdm.app.dashboard
